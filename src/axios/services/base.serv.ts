import {
  IProxyHttp,
  IConfigAdapter,
  HttpFactory,
} from '../core/'

export class BaseService {
  protected readonly  proxHttp: any;
  protected readonly configAdapter: any;

  constructor() {
    this.proxHttp = 
  }
}